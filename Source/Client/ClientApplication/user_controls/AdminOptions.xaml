<UserControl x:Class="ClientApplication.user_controls.AdminOptions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClientApplication.user_controls"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="200">
    <UserControl.Resources>
        <Style x:Key="Icon" TargetType="Image">
            <Setter Property="HorizontalAlignment" Value="Left"></Setter>
            <Setter Property="Margin" Value="10,10,10,10"></Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Button Name="cmAdminOptionsButton" Click="cmAdminOptionsButton_Click"  >
            <Image Source="../images/settings-icon.png" Style="{StaticResource Icon}"></Image>
            <Button.ContextMenu>
                <ContextMenu Name="cmAdminOptions">
                    <MenuItem Header="Was geht"></MenuItem>
                </ContextMenu>
            </Button.ContextMenu>
        </Button> 
    </Grid>
</UserControl>
